# Shell scripts must keep LF endings or bash fails with '\r: command not found',
# regardless of the cloner's core.autocrlf setting.
*.sh text eol=lf

# PowerShell is fine either way; normalise for clean diffs.
*.ps1 text eol=crlf

*.md   text
*.json text
*.js   text
*.ts   text
